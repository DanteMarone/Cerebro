PyQt5
requests
win10toast; sys_platform == 'win32'
sympy
plyer
keyring
pyttsx3
QScintilla
pyautogui
pynput
SpeechRecognition

# The `browsermcp` tool requires Node.js and the `browsermcp` package.
# Install it with: npm install -g @browsermcp/mcp
